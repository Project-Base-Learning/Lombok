<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteRule ^(.*)$ public/$1 [L]

    <FilesMatch "\.(env|json|lock|md|gitignore|gitattributes|yml|yaml|log)$">
        Order allow,deny
        Deny from all
    </FilesMatch>

</IfModule>
